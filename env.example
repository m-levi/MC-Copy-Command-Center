# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-here

# =============================================================================
# Vercel AI Gateway (PRIMARY - RECOMMENDED)
# =============================================================================
# Get your key from: https://vercel.com/ai-gateway
# Provides unified access to ALL AI models:
#   - Claude Sonnet 4.5, Haiku 4.5 (Anthropic)
#   - GPT-5, GPT-5 mini (OpenAI)
#   - Gemini 2.5 Flash (Google)
#   - And 100+ more models
# 
# Benefits:
#   - Single API key for all providers
#   - Automatic failovers
#   - Usage analytics & unified billing
#   - No need for individual provider API keys
AI_GATEWAY_API_KEY=your-vercel-ai-gateway-key-here

# =============================================================================
# Individual Provider Keys (OPTIONAL - BYOK)
# =============================================================================
# Only needed if you want to use your own provider accounts via BYOK 
# (Bring Your Own Key) for cost management or specific account features.
# With AI Gateway, these are OPTIONAL.

# OpenAI (for Whisper transcription only - not available via AI Gateway)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-key-here

# Anthropic (optional - use AI Gateway instead)
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# =============================================================================
# Email Service (Resend)
# =============================================================================
RESEND_API_KEY=re_your_resend_api_key_here
# sender email address (e.g., onboading@yourdomain.com)
EMAIL_FROM=onboarding@resend.dev

# =============================================================================
# Background Processing (Vercel Cron Jobs)
# =============================================================================
# Generate a random secret: openssl rand -hex 32
# Required for /api/cron/process-queue endpoint
CRON_SECRET=your-cron-secret-here

# Optional: Enable background queue processing (default: false)
# When enabled, AI messages are queued and processed by cron instead of streaming directly
ENABLE_MESSAGE_QUEUE=false

# Application URL (for email links)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# =============================================================================
# Supermemory (AI Memory Service)
# =============================================================================
# Get your key from: https://supermemory.ai
# Provides persistent AI memory per brand per user
SUPERMEMORY_API_KEY=your-supermemory-api-key-here
